{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "declaration": true,
    "outDir": "../lib",
    // We want to be very strict about which types source files have access to.
    // Typically they would have access to any types found in node_modules/@types.
    "types": []
  }
}
