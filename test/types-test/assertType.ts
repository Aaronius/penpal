// TODO Look into the expect-type npm package as a potential replacement.
const assertType = <T>(_value: T): void => {
  // Helper for checking type inference correctness
};

export default assertType;
